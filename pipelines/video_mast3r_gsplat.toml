title = "Video to Gaussian Splatting (MASt3R)"
description = "Estrae frame nitidi, calcola SfM con MASt3R e allena Gaussian Splatting."

[global_options]
filter_opacity_threshold = 0.05

[[steps]]
name = "Extraction"
method = "methods/preprocess/sharp_frames.toml"
# Questa chiave userà il valore passato via CLI --input
[steps.inputs]
video = "{{context.input_file}}"

[[steps]]
name = "StructureFromMotion"
method = "methods/sfm/mast3r_sfm.toml"
[steps.inputs]
# Prende l'output dello step precedente
images_dir = "{{context.Extraction_sharp_images}}"
[steps.kwargs]
scenegraph_type = "swin"
# Definiamo la chiave con cui salvare l'output nel contesto per il prossimo step
output_key = "sfm_ready_path"
primary_output = "model_dir"


[[steps]]
name = "Training"
method = "methods/splat/gsplat.toml"
[steps.inputs]
#{StepName}_{output_name}
source_path = "{{context.StructureFromMotion_model_dir}}"
[steps.kwargs]
iterations = 100000

# MODULAR_GS_SIGNATURE: 136b27cf7f2a94cb5e5da40983866bc1b3b425ebc740ff5bf83ebe880c452d6bcb9a2309595103f6dc084776afa20cf52f2a55e48ff520aadebcf88fa015fa1c
