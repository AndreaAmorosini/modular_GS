title = "Gaussian Splatting (Original)"

[environment]
python_version = "3.10"
cuda = "11.8"
# Isolamento per evitare conflitti con pacchetti globali
PYTHONNOUSERSITE = "1"

[installation]
channels = ["pytorch", "nvidia", "conda-forge"]

# Aggiunta URL per le wheel precompilate (come in nerfstudio.toml)
wheels_base_url = "https://huggingface.co/datasets/AterMors/gs_wheels/resolve/main"

dependencies = [
    "pytorch=2.1.2",
    "torchvision=0.16.2",
    # Compilatori di sistema (potrebbero servire ancora per dipendenze minori, meglio lasciarli)
    "gxx_linux-64",
    "gcc_linux-64",
    "make",
    "cmake"
]

# git_repos = [
#     # Manteniamo il clone per avere accesso agli script py (train.py, sif_to_splat.py, etc)
#     { url = "https://github.com/graphdeco-inria/gaussian-splatting", recursive = true }
# ]

pip_dependencies = [
    "plyfile",
    "tqdm",
    "argparse",
    # Usiamo le versioni precompilate invece di compilare localmente
    "@wheel:diff-gaussian-rasterization",
    "@wheel:simple_knn"
]

build_commands = [
    # Rimuoviamo la compilazione manuale dei sottomoduli poichè installiamo le wheel
]

[[installation.git_repos]]
url = "https://github.com/graphdeco-inria/gaussian-splatting"
branch = "main"
path = "gaussian_splatting"
recursive = true
